<div class="min-h-screen bg-gray-100 flex flex-col items-center justify-items-start px-4">
  <!-- Logo (above card) -->
  <div class="-mb-16 -mt-20">
    <%= image_tag "trackr.png", alt: "Trackr Logo", class: "h-60 w-auto drop-shadow-lg" %>
  </div>

  <!-- Forgot Password Card -->
  <div class="w-full max-w-md bg-white p-6 rounded-2xl shadow-lg">
    <h2 class="text-2xl font-bold text-center mb-5 text-gray-800">Forgot your password?</h2>
    <p class="text-center text-sm text-gray-600 mb-6">
      Enter your email address and we’ll send you instructions to reset your password.
    </p>
<% if resource.errors.any? %>
  <div class="mb-4 p-4 border-l-4 border-red-600 bg-red-100 text-red-800 rounded">
    <h4 class="font-semibold mb-2">Oops! </h4>
    <ul class="list-disc list-inside space-y-1">
      <% resource.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="mb-4">
        <%= f.label :email, class: "block mb-1 text-sm font-medium text-gray-700" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email",
              class: "w-full p-2.5 border border-gray-300 rounded-md focus:ring-green-600 focus:border-green-600 text-sm" %>
      </div>

      <div>
        <%= f.submit "Send reset instructions",
              class: "w-full cursor-pointer bg-green-600 text-white py-2.5 rounded-md hover:bg-green-700 font-semibold text-sm transition" %>
      </div>
    <% end %>

    <p class="mt-6 text-center text-sm text-gray-600">
      <%= link_to "Back to login", new_session_path(resource_name),
            class: "text-green-600 font-semibold hover:underline" %>
    </p>
  </div>
</div>
