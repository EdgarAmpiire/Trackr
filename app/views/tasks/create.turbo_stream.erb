<%= turbo_stream.prepend "flash", partial: "shared/flash" %>
<%= turbo_stream.replace "new_task_form" do %>
  <turbo-frame id="redirect">
    <script>
      window.location.href = "<%= task_path(@task) %>";
    </script>
  </turbo-frame>
<% end %>
