<%= form_with(model: [task, comment], data: { turbo_stream: true }, class: "mt-4") do |form| %>
  <%= form.text_area :content, class: "w-full border rounded p-2", placeholder: "Add a comment..." %>
  <%= form.submit "Post", class: "bg-blue-600 text-white px-3 py-1 rounded mt-2" %>
<% end %>
